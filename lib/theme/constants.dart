import 'package:flutter/material.dart';

const defaultMargin = 12.0;
const defaultPadding = 18.0;
const defaultBorderRadius = 24.0;

const kDefaultMargin = EdgeInsets.all(defaultMargin);
const kDefaultPadding = EdgeInsets.all(defaultPadding);
const kDefaultBorderRadius = Radius.circular(defaultBorderRadius);
